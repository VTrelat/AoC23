import * as fs from "fs";
console.log(((arr) => arr[0].map((_, i) => arr.map((row) => row[i])))(fs.readFileSync("/Users/vincent/Documents/AoC23/inputs/input6.txt", { encoding: "utf-8", flag: "r" }).trim().split("\n").map((l) =>l.split(":")[1].trim().split(" ").filter((w) => w.length > 0).map(Number))).map(([t, d]) => {const s = Math.sqrt(t * t - 4 * d);return Math.floor((t + s) / 2) - Math.ceil((t - s) / 2) + 1;}).reduce((a, b) => a * b, 1));